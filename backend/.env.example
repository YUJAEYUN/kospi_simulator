# 토스증권 Open API (WTS 설정 > Open API에서 발급). 이 VM의 고정 IP를
# 토스 개발자센터 허용 IP 목록에 등록해야 합니다.
TOSS_CLIENT_ID=
TOSS_CLIENT_SECRET=

# Next.js(Vercel) 쪽 BACKEND_SECRET과 동일한 값을 넣으세요. 아무 값이나
# 넣지 말고 openssl rand -hex 32 같은 걸로 충분히 무작위하게 생성하세요.
BACKEND_SECRET=

# 선택 (기본값 사용해도 됨)
PORT=4000
POLL_INTERVAL_MS=2500
